#!/bin/bash

# GAMES {
	# steam
	echo steam > /tmp/regataos-prime/noapps-hybrid-graphics-steam.txt
	# pcsx2
	echo pcsx2 > /tmp/regataos-prime/noapps-hybrid-graphics-pcsx2.txt
	# dolphin-emu
	echo dolphin-emu > /tmp/regataos-prime/noapps-hybrid-graphics-dolphin-emu.txt
	# supertuxkart
	echo supertuxkart > /tmp/regataos-prime/noapps-hybrid-graphics-supertuxkart.txt
	# retroarch
	echo retroarch > /tmp/regataos-prime/noapps-hybrid-graphics-retroarch.txt
	# lutris
	echo lutris > /tmp/regataos-prime/noapps-hybrid-graphics-lutris.txt
# }

# PHOTO {
	# gimp
	echo gimp > /tmp/regataos-prime/noapps-hybrid-graphics-gimp.txt
	# darktable
	echo darktable > /tmp/regataos-prime/noapps-hybrid-graphics-darktable.txt
	# inkscape
	echo inkscape > /tmp/regataos-prime/noapps-hybrid-graphics-inkscape.txt
	# krita
	echo krita > /tmp/regataos-prime/noapps-hybrid-graphics-krita.txt
	# aftershot
	echo aftershot > /tmp/regataos-prime/noapps-hybrid-graphics-aftershot.txt
	# gravit-designer
	echo gravit-designer > /tmp/regataos-prime/noapps-hybrid-graphics-gravit-designer.txt
# }

# 3D MODELING {
	# blender
	echo blender > /tmp/regataos-prime/noapps-hybrid-graphics-blender.txt
# }

# PHOTO {
	# simplescreenrecorder
	echo simplescreenrecorder > /tmp/regataos-prime/noapps-hybrid-graphics-simplescreenrecorder.txt
	# lightworks
	echo lightworks > /tmp/regataos-prime/noapps-hybrid-graphics-lightworks.txt
	# kdenlive
	echo kdenlive > /tmp/regataos-prime/noapps-hybrid-graphics-kdenlive.txt
	# obs
	echo obs > /tmp/regataos-prime/noapps-hybrid-graphics-obs.txt
	# nuke
	echo nuke > /tmp/regataos-prime/noapps-hybrid-graphics-nuke.txt
	# shotcut
	echo shotcut > /tmp/regataos-prime/noapps-hybrid-graphics-shotcut.txt
	# davinci-resolve
	echo davinci-resolve > /tmp/regataos-prime/noapps-hybrid-graphics-davinci-resolve.txt
	# fusionforlinux
	echo fusionforlinux > /tmp/regataos-prime/noapps-hybrid-graphics-fusionforlinux.txt
	# openshot
	echo openshot > /tmp/regataos-prime/noapps-hybrid-graphics-openshot.txt
	# natron
	echo natron > /tmp/regataos-prime/noapps-hybrid-graphics-natron.txt
# }
